# Memora
Memora is a note taking app that is built on the MERN stack, for styling tailwindcss is used with themes from [daisyUI](https://daisyui.com/).
The project is split into Frontend and Backend. Axios is used to fetch the APIs from the backend.
This project also implements rate limiting with the help of Redis using [upstash](https://upstash.com/).
